kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

# Dokka V2 Migration - Using helpers for compatibility
org.jetbrains.dokka.experimental.gradle.pluginMode=V2EnabledWithHelpers

# Publishing configuration
# You can override these in ~/.gradle/gradle.properties or local.properties
# For GitHub Packages publishing:
# gpr.user=your-github-username
# gpr.key=your-github-token

# For Maven Central publishing:
# ossrh.username=your-sonatype-username
# ossrh.password=your-sonatype-password
# signing.key=your-pgp-private-key
# signing.password=your-pgp-key-password 